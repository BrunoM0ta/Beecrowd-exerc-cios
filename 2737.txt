(SELECT name, customers_number FROM LAWYERS ORDER BY customers_number DESC
LIMIT 1)
UNION ALL
(SELECT name, customers_number
FROM LAWYERS
ORDER BY customers_number ASC
LIMIT 1)
UNION ALL
(SELECT 'Average', round(AVG(customers_number), 0)
FROM LAWYERS);